import Config

# App-level runtime.exs — loaded by Mix in dev/test only.
# In production releases, ONLY the umbrella root config/runtime.exs is used.
# Any config that must apply in production belongs in the umbrella root file.
#
# This file exists for dev/test overrides that don't apply to releases.
