-- Generated by mix proto.sync — DO NOT EDIT MANUALLY.
-- Source: stacks/common/v1/marketplace.proto (Transaction)
{{ config(materialized='view') }}

select
    id,
    listing_id,
    offer_id,
    buyer_id,
    seller_id,
    amount_cents,
    currency,
    payment_status,
    shipping_status,
    shipping_cost_cents,
    completed_at,
    created_at
from {{ source('op', 'transactions') }}
