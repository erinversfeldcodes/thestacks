# .gitleaks.toml — gitleaks configuration for The Stacks
# All findings below are false positives in vendored/generated code.

[extend]
useDefault = true

[[allowlists]]
description = "Ignore vendored deps and compiled build artefacts"
paths = [
    '''deps/''',
    '''_build/''',
]
