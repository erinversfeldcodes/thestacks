# Issue #285: Platform Discovery Search Sectioning

## Summary
Implement the US-1.5.3 platform-wide discovery experience de-scoped from Issue #115: search results split into "Your Collection" vs "On the Platform" sections, with multi-source results (marketplace listings, partner inventory, other users' public-bookshelf placements) and contextual source labels.

## User Stories
- US-1.5.3 — Platform-Wide Discovery Search (de-scoped from #115 at epic kickoff 2026-07-23; the one shipped AC — platform-wide visibility filtering on book results — stays validated under #115/US-1.5.1)

## Goal
A user searching the platform sees their own books separated from discoverable platform inventory, each result labelled by source (own shelf, marketplace, partner, public shelf).

## Scope Check
- Does this issue touch more than 3 controllers? No (SearchController; possibly CatalogueController alignment).
- Does this issue add more than 2 new endpoints? No (extends `GET /api/search` response shape).
- Does this issue exceed ~300 lines of production code? Borderline — server sectioning + Elm sections; split the Elm slice if it grows.
- Does this issue combine unrelated concerns? No.

## Wiring
Router wiring: includes wiring (extends existing search surface), user-facing on completion.

## Feature-Completeness Pre-Check

| User Story | Happy-path hops (file:line) | Live-drive result | Verdict | Resolution |
|-----------|------------------------------|-------------------|---------|------------|
| US-1.5.3 — Discovery sectioning | `search_controller.ex:index` → collection: `Shelving.search_collection/3` · platform: `Books.search_books/2` → `Visibility.can_view?` · labels: `Marketplace.active_listing_labels/1` (listed+price, precedence) + `Shelving.looking_for_home_labels/1` (LFH always-visible) → `ProtoJSON.search_hit/2` → Elm: `Api.searchResponseDecoder` → `SearchSections` → `Page.Search.viewCollectionSection`/`viewPlatformSection` (shelf line + LFH/listed labels) | **Live-driven 2026-07-24** against the running #285 stack (:4000) as an authenticated minted viewer searching `Book`, rendered DOM captured: `Your Collection` → row "The Book of Legendary Lands" label **"On your Library shelf"**; `On the Platform` → row "The Book of Sand" label **"Listed by live_seller_a87372 for R120"** + row "The Book of Laughter and Forgetting" label (none). Collection dedup, empty-collection hide, and per-section sort all asserted green in `search.spec.ts` (3 sectioning specs). | ✅ implemented (built end-to-end + observed live) | Done (Phase 4 E2E live drive) |

Verdict: ✅ implemented (built end-to-end + observed live) · 🟡 partial (enumerate missing hops) · ❌ missing (build in-scope or de-scope).

## Technical Requirements
- Server: section search results by whether the viewer has an active placement ("Your Collection") vs platform-visible sources ("On the Platform"); placement/bookshelf-level platform-visibility scoping per US-1.5.3 §5 (join `op.bookshelf_placements`/`op.bookshelves`, respect the Audience/visibility ladder — coordinate with ADR-018/#209).
- Sources: marketplace listings, partner inventory, other users' public bookshelves; contextual labels per source.
- Elm `Page.Search`: sectioned rendering; coexists with the #217 people-search section.
- Related dead code: see #286 (`Books.search_platform/2` is currently unwired and does not filter by visibility — resolve there or absorb here, but not both).

## Reviewer Context
- The unified Audience model refactor (#209, blocked on #122) may land first — align the visibility scoping with whichever model is current.
- Partners never see user data; sectioning must not leak private placements to other viewers.

## Test Audit
[Baseline to be generated by the `test-audit` skill when this issue is picked up.]

## Definition of Done
- [x] Sectioned results with source labels render for a live search — evidence: live drive + Playwright _(Phase 4)_ — `search.spec.ts` "Sectioned search (#285)" (3 specs, all green on :4000): collection hit renders under "Your Collection" with **"On your Library shelf"** + deduped off platform + opens overlay (#289); active listing renders under "On the Platform" with **"Listed by <handle> for R120"** (DOM bound to the live wire probe); empty-collection viewer sees platform-only + per-section sort. Live-drive DOM capture recorded in the Pre-Check cell above.
- [x] Placement/bookshelf-level visibility scoping enforced — evidence: context tests incl. exclusion cases _(backend, Phase 2)_ — `search_controller_test.exs` "another user's private library placement leaks no label or provenance" (source/owner_handle empty) + `shelving_test.exs` "ignores active listing_status on a non-LFH bookshelf"; platform side retains `Visibility.can_view?`
- [x] **Feature-Completeness Pre-Check (above) is ✅ for every named user story** — US-1.5.3 driven live on :4000 (both sections + shelf line + listed label observed in the rendered DOM)
- [x] Every behaviour has a validation path — backend context/controller tests (Phase 2) + Elm `SearchProgramTest` (Phase 3) + `search.spec.ts` E2E live drive (Phase 4); the LFH label is proven at the Elm layer only (not independently reachable via API — see reachability note below)
- [x] Tests written and passing _(backend: 234/0 across shelving+marketplace+search+proto_json; Elm: 1042/0 full elm-test incl. 10 new sectioning `SearchProgramTest` cases, baseline 1032; E2E: `search.spec.ts` **17/0** — setup(2) + chromium(15), incl. 3 new "Sectioned search (#285)" specs — against the live #285 stack on :4000; `scripts/check-e2e-vacuous-guards.sh` clean; prettier clean)_
- [ ] Standards compliance verified (`just verify` passes)
- [ ] **Test audit is GREEN**
- [ ] **`completion-audit` skill passed on the integrated branch**
- [ ] **Meets the Completion Bar** (`docs/agents/standards/completion-bar.md`)

### Phases 1+2 evidence (elixir/protobuf specialist)
- **Phase 1 — Contract:** `proto/stacks/api/v1/book_responses.proto` — additive `SearchResponse.collection` (field 4) + `platform_hits` (field 5), each `repeated SearchHit`; new `SearchHit {book=1, source=2, owner_handle=3, price=4, bookshelf_name=5}`; `results` (field 3) retained for backward compat. `bookshelf_name` (field 5, added on owner approval) carries the shelf a collection hit sits on — populated for COLLECTION hits only; platform_hits leave it "". `buf lint` exit 0. `mix proto.sync --check` → "All generated files are up to date." `scripts/gen-elm-proto.sh` regenerated (generated Elm carries `SearchHit`/`collection`/`platformHits`/`bookshelfName` with `D.oneOf [..., D.succeed …]` back-compat fallbacks). Full `elm-test` → **1032 passed / 0 failed** (baseline 1031; the +1 is a concurrent frontend agent's `BookDetailProgramTest.elm`, not this change — this change adds no Elm tests, only additive decoders).
- **Phase 2 — Backend:** `Shelving.search_collection/3` (own active-placement title matches, distinct, tagged with the bookshelf name, `plainto_tsquery` bound param, no sanitiser), `Shelving.looking_for_home_labels/1` (LFH + `listing_status:"active"` marketplace-exception → owner handle), `Marketplace.active_listing_labels/1` (active listing → seller handle + `format_price/2`), `ProtoJSON.search_hit/2` (incl. `bookshelf_name`), `SearchController.index` assembly (collection hits carry `bookshelf_name`; platform_hits + `results` retained; collection de-duplicated out of platform_hits; "listed" precedence over "looking_for_home"). Scoped tests: **236 tests, 0 failures** (shelving + marketplace + search_controller + proto_json) — incl. "collection hits carry their bookshelf name; platform hits leave it empty". `mix format --check-formatted` clean; `mix credo --strict` — "found no issues".
- **GDPR lens (verdict: PASS):** No new persisted data — all three helpers are read-only queries; the proto `SearchHit` is a response type, not a table; no migration/schema/field added. Erasure (`GDPR.Deletion.delete_user_data/1`) and export (`GDPR.Export`) paths are **unchanged** — nothing new to reach. The only PII-adjacent field surfaced is `owner_handle`, which rides the **existing** public-handle exposure (already public via `/u/:handle`, people-search cards, blog author_handle) and is emitted **only** for discoverable-by-design provenance: active marketplace listings (US-7.2, intentionally public) and always-visible `looking_for_home` placements (the `Visibility.marketplace_exception?` rule). It is **never** emitted for ordinary/private placements — asserted by `search_controller_test.exs` ("another user's private library placement leaks no label or provenance": source="" and owner_handle="") and `shelving_test.exs` ("ignores active listing_status on a non-LFH bookshelf"). No event_log/audit/dbt write on the search path (pure read).

## Dependencies
- Issue #115 (search E2E harness)
- Issue #286 (search_platform/2 dead-code resolution)
- ADR-018 / Issue #209 (Audience model) for visibility semantics

## Agent Assignment
`elixir-agent` (sectioning/scoping) + `elm-agent` (sectioned UI).

## Progress Notes
- 2026-07-23 — Created at #115/#114/#113 epic kickoff: US-1.5.3 sectioning/multi-source de-scoped from #115 (unimplemented; #115 is test-only).
- 2026-07-24 — Phases 1+2 (contract + backend) complete. Owner-confirmed scope: "Your Collection" (placement-scoped title search) + "On the Platform" (existing search_books) with discoverable-by-design labels — active listings ("listed" + handle + price, precedence) and always-visible looking_for_home placements ("looking_for_home" + handle). Additive proto contract (SearchHit + collection/platform_hits, results retained), both codegen targets regenerated (proto.sync + elm proto), buf lint clean, elm-test 1032/0. Backend: 234/0 across shelving/marketplace/search/proto_json; format + credo clean. GDPR lens PASS (read-only, no new persisted PII; owner_handle rides existing public-handle exposure, only for always-visible surfaces; private placements leak nothing — asserted). Note: `Return bookshelf name per hit` (Phase-2 note) was NOT in the initial owner-confirmed 4-field SearchHit contract — flagged to `main` rather than silently deviating. Phases 3 (Elm) + 4 (E2E) remain.
- 2026-07-24 — Owner approved the follow-up: added additive `SearchHit.bookshelf_name = 5` (US-1.5.1's collection "where"). Populated for COLLECTION hits only; platform_hits leave it "". `search_collection/3` now returns `%{book, bookshelf_name}`. Full loop re-run green: buf lint exit 0, `proto.sync --check` up to date, elm proto regenerated, `elm-test` 1032/0, scoped Elixir 236/0, format + credo clean. Sectioning tests extended to assert the shelf name on a collection hit and "" on a platform hit.
- 2026-07-24 — Phase 3 (Elm) complete. `Api.searchBooks`/`searchResponseDecoder` migrated off the flat `results` list to a typed `SearchSections { collection : List CollectionHit, platform : List PlatformHit }` (new `Api.CollectionHit`/`PlatformHit` carry the shelf/source/handle/price label data; mapped via `fromProtoBook`). `Page.Search` now renders "Your Collection" (per-hit "On your <Shelf> shelf", raw bookshelf name humanised to the canonical Upload/Catalogue labels) above "On the Platform" ("Looking for a home on <handle>'s shelf" / "Listed by <handle> for <price>", label shown only when `source` non-empty); each section hides when empty (platform-only for empty collections, collection-only when platform empty); overall empty/error/hint states + #290 copy intact; sort + year-filter apply WITHIN each section (`applyYearFilter`/`sortBooks` generalised to `{ a | book : Book }`, run over each list); rows stay #289 clickable `<button>`s with stable `search-result-<id>` in both sections. Test-first: 8 new `SearchProgramTest` cases failed on real assertions against a minimal flat view (missing headings/labels/section classes) before the sectioned view landed. Final: full `elm-test` **1042/0** (baseline 1032 + 10 new sectioning cases, incl. `SearchTest` unchanged), elm-format --validate clean, elm-review clean, `elm make --optimize` 0 warnings, assets rebuilt (`apps/core/assets && npm run deploy` → `priv/static/assets/app.js` carries "Your Collection"/"On the Platform"/shelf+listing labels/`search-section--collection`/`search-result__label`). `TestHelpers.searchEffects` unchanged (still reuses `Api.searchResponseDecoder` — #292 no-drift). Smoke: the stack currently on :4000 runs a PRE-#285 binary (its `/api/search` response has no `collection`/`platform_hits` keys), so live sectioning can't be observed against it and the shared server was not restarted (many agents active); the decoder tolerates the old shape (both sections empty → "Nothing on the shelves matches that — yet.", no crash). Live E2E is Phase 4. Files: `frontend/src/Api.elm`, `frontend/src/Page/Search.elm`, `frontend/tests/Page/SearchProgramTest.elm`, `frontend/css/main.css`.
- 2026-07-24 — Phase 4 (E2E + live drive) complete. Restarted the shared Phoenix on :4000 with the current #285 code (`STACKS_E2E_TEST_HELPERS=1 AGE_GATING_ENABLED=true MIX_ENV=dev`, stacks_dev) — the prior binary had no `collection`/`platform_hits` keys; confirmed the authed `GET /api/search` now returns them. Added a "Sectioned search (#285)" describe to `e2e/tests/search.spec.ts` (fresh-context, minted throwaway users via `mintOrSkip`/`injectSession`, so state is built per-spec and deterministic regardless of shared-seed drift): (1) a placed book renders under **Your Collection** with **"On your Library shelf"**, is **deduped off the platform**, and its row **opens the book overlay** (#289 composition); (2) an active marketplace listing (place → `POST /api/listings` → `PUT …/activate`, R120 ZAR) renders under **On the Platform** with **"Listed by <handle> for R120"**, the DOM assertion bound to a live `/api/search` wire probe for the winning seller handle, and a non-owner viewer has **no** collection section; (3) an **empty-collection** viewer sees platform-only and **sort re-orders within the platform section** (full title/author/year 3-way). Also fixed the pre-existing flat-order sort spec, which the now-live sectioning legitimately broke: the suite `search` user OWNS "The Book of Laughter and Forgetting" (seeded on library), so it moves into "Your Collection" and the global `.search-result__title` order is no longer a single sorted list — rescoped its order assertions to the platform section (collection stays put, platform's two works flip title↔author deterministically). **17/0** `search.spec.ts` (setup 2 + chromium 15) against :4000; `check-e2e-vacuous-guards.sh` clean; prettier clean. **LIVE DRIVE (proving gate):** an authenticated viewer searching `Book` rendered `Your Collection` → "The Book of Legendary Lands" / "On your Library shelf" and `On the Platform` → "The Book of Sand" / "Listed by live_seller_a87372 for R120" + "The Book of Laughter and Forgetting" / (no label) — captured from the live DOM.
- 2026-07-24 — **Reachability flag (LFH label):** the `looking_for_home` platform label ("Looking for a home on <handle>'s shelf") is **NOT independently reachable via any public API path**, so Phase-4 E2E asserts the **"listed"** variant instead (per the plan's "assert whichever label state IS reachable — do not fabricate"). Root cause: `Shelving.looking_for_home_labels/1` requires a `looking_for_home` placement whose `listing_status = "active"`, and the ONLY writer of that column is `Marketplace.activate_listing/2`, which simultaneously creates the active `Listing` row that `SearchController.discovery_labels/1` merges with **"listed" precedence** (`Map.merge(lfh, listed)`) — so activation always surfaces "listed", never a bare "looking_for_home". Seeds set `listing_mode`/`listing_price_cents` on LFH placements but never `listing_status`. The LFH label render remains covered at the Elm layer (`SearchProgramTest`). Follow-up (not in #285 scope): if a bare-LFH advert label is desired product-wise, it needs a distinct activation path that sets `listing_status:"active"` on an LFH placement without a competing marketplace `Listing` (or a precedence rule that surfaces LFH when the listing is absent) — flagged to `main`.
